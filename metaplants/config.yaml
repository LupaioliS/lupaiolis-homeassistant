name: "MetaPlants"
version: "1.1.0"
slug: "metaplants"
description: "Plant manager per Home Assistant - gestisci irrigazione e fertilizzazione con MQTT"
arch:
  - aarch64
  - amd64
ingress: true
ingress_port: 3000
panel_icon: "mdi:flower"
map:
  - data:rw
options:
  mqtt_url: "mqtt://core-mosquitto:1883"
  mqtt_user: ""
  mqtt_pass: ""
  ha_discovery_prefix: "homeassistant"
  lang: "it"
schema:
  mqtt_url: str
  mqtt_user: str?
  mqtt_pass: password?
  ha_discovery_prefix: str
  lang: "list(it|en)"